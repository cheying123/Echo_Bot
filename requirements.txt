# QQ AI Bot — 依赖
# ====================

# 核心
pydantic>=2.0.0
httpx>=0.27.0
aiosqlite>=0.20.0
pyyaml>=6.0

# WebSocket 服务器（对接 go-cqhttp）
websockets>=12.0

# .env 文件加载（生产部署用）
python-dotenv>=1.0.0

# 容错 JSON 解析
json5>=0.9.0

# 深度学习语义检索（可选，大幅提升台词匹配质量）
# pip install sentence-transformers numpy
# sentence-transformers>=3.0.0
# numpy>=1.24.0
